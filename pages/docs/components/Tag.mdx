import Heart from "@sonnat/icons/Heart";
import Tag from "@sonnat/ui/Tag";
import PageEditButton from "components/PageEditButton";
import AnchorButton from "components/AnchorButton";
import WithSidebar from "components/layouts/WithSidebar";
import DemoBox from "components/DemoBox";
import Head from "next/head";
import { defaultKeywordsMetaContent, siteFullAddress } from "sharedVars";
import {
  setCanonicalMeta,
  setDescriptionMeta,
  setKeywordsMeta,
  setTitleMeta
} from "utils";

export const meta = {
  title: "Tag",
  description:
    "Tags mission is to label, categorize, or organize items using keywords that describe them.",
  getLayout: () => page => <WithSidebar>{page}</WithSidebar>
};

<Head>
  {setTitleMeta(
    `${meta.title} | Sonnat Developer Tools, React Components & Resources`
  )}
  {setDescriptionMeta(meta.description)}
  {setCanonicalMeta(`${siteFullAddress}/docs/components/${meta.title}`)}
  {setKeywordsMeta([...defaultKeywordsMetaContent, "tag", "برچسب", "تگ"])}
</Head>

<h1>
  <span>{meta.title}</span>
  <AnchorButton anchorId="overview" />
  <PageEditButton href={`pages/docs/components/${meta.title}.mdx`} />
</h1>

<p>{meta.description}</p>

<h2>
  <span>Filled Tags</span>
  <AnchorButton anchorId="filled-tags" />
</h2>

<p>Filled/Contained is the default style for the tags.</p>

<DemoBox
  code={`<Tag label="Default" color="default" />
<Tag label="Primary" color="primary" />
<Tag label="Secondary" color="secondary" />
<Tag label="Success" color="success" />
<Tag label="Error" color="error" />
<Tag label="Warning" color="warning" />
<Tag label="Info" color="info" />`}
>
  <Tag className="demoSubject" label="Default" color="default" />
  <Tag className="demoSubject" label="Primary" color="primary" />
  <Tag className="demoSubject" label="Secondary" color="secondary" />
  <Tag className="demoSubject" label="Success" color="success" />
  <Tag className="demoSubject" label="Error" color="error" />
  <Tag className="demoSubject" label="Warning" color="warning" />
  <Tag className="demoSubject" label="Info" color="info" />
</DemoBox>

<h2>
  <span>Outlined Tags</span>
  <AnchorButton anchorId="outlined-tags" />
</h2>

<p>Outlined tags offer an alternative style.</p>

<DemoBox
  code={`<Tag variant="outlined" label="Default" color="default" />
<Tag variant="outlined" label="Primary" color="primary" />
<Tag variant="outlined" label="Secondary" color="secondary" />
<Tag variant="outlined" label="Success" color="success" />
<Tag variant="outlined" label="Error" color="error" />
<Tag variant="outlined" label="Warning" color="warning" />
<Tag variant="outlined" label="Info" color="info" />`}
>
  <Tag
    variant="outlined"
    className="demoSubject"
    label="Default"
    color="default"
  />
  <Tag
    variant="outlined"
    className="demoSubject"
    label="Primary"
    color="primary"
  />
  <Tag
    variant="outlined"
    className="demoSubject"
    label="Secondary"
    color="secondary"
  />
  <Tag
    variant="outlined"
    className="demoSubject"
    label="Success"
    color="success"
  />
  <Tag variant="outlined" className="demoSubject" label="Error" color="error" />
  <Tag
    variant="outlined"
    className="demoSubject"
    label="Warning"
    color="warning"
  />
  <Tag variant="outlined" className="demoSubject" label="Info" color="info" />
</DemoBox>

<h2>
  <span>With Leading Icon</span>
  <AnchorButton anchorId="with-leading-icon" />
</h2>

<p>
  Tag components can contain an icon. This is done by setting the{" "}
  <inlineCode>icon</inlineCode> property.
</p>

<DemoBox
  code={[
    '<Tag label="Filled" icon={<Heart />} />',
    '<Tag label="Outlined" variant="outlined" icon={<Heart />} />'
  ].join("\n")}
>
  <div className="demoSubject">
    <Tag label="Filled" icon={<Heart />} />
  </div>
  <div className="demoSubject">
    <Tag label="Outlined" variant="outlined" icon={<Heart />} />
  </div>
</DemoBox>

<h2>
  <span>Denser</span>
  <AnchorButton anchorId="denser" />
</h2>

<p>
  To have denser tags, use the <inlineCode>dense</inlineCode> property.
</p>

<DemoBox
  code={`<Tag dense label="Default" color="default" />
<Tag dense label="Primary" color="primary" />
<Tag dense label="Secondary" color="secondary" />
<Tag dense label="Success" color="success" />
<Tag dense label="Error" color="error" />
<Tag dense label="Warning" color="warning" />
<Tag dense label="Info" color="info" />`}
>
  <Tag dense className="demoSubject" label="Default" color="default" />
  <Tag dense className="demoSubject" label="Primary" color="primary" />
  <Tag dense className="demoSubject" label="Secondary" color="secondary" />
  <Tag dense className="demoSubject" label="Success" color="success" />
  <Tag dense className="demoSubject" label="Error" color="error" />
  <Tag dense className="demoSubject" label="Warning" color="warning" />
  <Tag dense className="demoSubject" label="Info" color="info" />
</DemoBox>

<h2>
  <span>Removable Tags</span>
  <AnchorButton anchorId="removable-tags" />
</h2>

<p>
  To have removable tags, assign a callback to the{" "}
  <inlineCode>onRemove</inlineCode> property.
</p>

<DemoBox
  code={`<Tag onRemove={() => {}} label="Default" color="default" />
<Tag onRemove={() => {}} label="Primary" color="primary" />
<Tag onRemove={() => {}} label="Secondary" color="secondary" />
<Tag onRemove={() => {}} label="Success" color="success" />
<Tag onRemove={() => {}} label="Error" color="error" />
<Tag onRemove={() => {}} label="Warning" color="warning" />
<Tag onRemove={() => {}} label="Info" color="info" />`}
>
  <Tag
    onRemove={() => {}}
    className="demoSubject"
    label="Default"
    color="default"
  />
  <Tag
    onRemove={() => {}}
    className="demoSubject"
    label="Primary"
    color="primary"
  />
  <Tag
    onRemove={() => {}}
    className="demoSubject"
    label="Secondary"
    color="secondary"
  />
  <Tag
    onRemove={() => {}}
    className="demoSubject"
    label="Success"
    color="success"
  />
  <Tag
    onRemove={() => {}}
    className="demoSubject"
    label="Error"
    color="error"
  />
  <Tag
    onRemove={() => {}}
    className="demoSubject"
    label="Warning"
    color="warning"
  />
  <Tag onRemove={() => {}} className="demoSubject" label="Info" color="info" />
</DemoBox>

<h2>
  <span>API</span>
  <AnchorButton anchorId="api" />
</h2>

<p>Learn more about the properties and the customization points.</p>

<h3>
  <span>Usage</span>
  <AnchorButton anchorId="usage" />
</h3>

```typescript
import Tag from "@sonnat/ui/Tag";

//or
import { Tag } from "@sonnat/ui";
```

<h3>
  <span>Properties</span>
  <AnchorButton anchorId="properties" />
</h3>

<p>
  Note that the documentation avoids mentioning all the native props (there are
  a lot) in this section of the components.
</p>

<table cols={4}>
  <caption>Tag Properties</caption>
  <thead>
    <tr>
      <th>Name</th>
      <th>Type</th>
      <th>Default</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>
        label
        <br />
        <mark>required</mark>
      </td>
      <td>string</td>
      <td>-</td>
      <td>The content of the tag.</td>
    </tr>
    <tr>
      <td>className</td>
      <td>string</td>
      <td>-</td>
      <td>
        Append to the class names applied to the component so you can override
        or extend the styles.
      </td>
    </tr>
    <tr>
      <td>variant</td>
      <td>
        "filled"
        <br />| "outlined"
      </td>
      <td>"filled"</td>
      <td>The variant of the tag.</td>
    </tr>
    <tr>
      <td>color</td>
      <td>
        "default"
        <br />| "primary"
        <br />| "secondary"
        <br />| "success"
        <br />| "error"
        <br />| "warning"
        <br />| "info"
      </td>
      <td>"filled"</td>
      <td>The color of the tag.</td>
    </tr>
    <tr>
      <td>icon</td>
      <td>node</td>
      <td>-</td>
      <td>The leading icon element placed before the label.</td>
    </tr>
    <tr>
      <td>dense</td>
      <td>boolean</td>
      <td>false</td>
      <td>
        If <inlineCode>true</inlineCode>, will make the tag denser.
      </td>
    </tr>
    <tr>
      <td>onRemove</td>
      <td>function</td>
      <td>-</td>
      <td>
        The callback fires when the tag's remove button is clicked.
        <br />
        <em>
          <strong>Signature:</strong>
        </em>{" "}
        <inlineCode>
          {`function(event: React.MouseEvent<HTMLDivElement, MouseEvent>) => void`}
        </inlineCode>
      </td>
    </tr>
  </tbody>
  <tfoot>
    <tr>
      <td colSpan="4">
        <ul>
          <li>
            The <inlineCode>ref</inlineCode> is forwarded to the root element.
          </li>
          <li>
            Any other properties supplied will be provided to the root element.
          </li>
        </ul>
      </td>
    </tr>
  </tfoot>
</table>
